using OpenTimelapseSort.Contexts;

namespace OpenTimelapseSort.Database
{
    class DBInitializer
    {
        // class that initializes the database on first use or on deletion ( set preferences only )
        public static void InitDatabase(PreferencesContext preferences)
        {
            // do what ya gotta do
        }
    }
}
