using OpenTimelapseSort.Contexts;
using System;
using System.Collections.Generic;
using System.Text;

namespace OpenTimelapseSort.Database
{
    class DBInitializer
    {
        // class that inits the database on first use or on deletion ( set preferences only )
        public static void InitDatabase(PreferencesContext preferences)
        {
            // do what ya gotta do
        }
    }
}
